{
  "name": "polypage-web-translator",
  "displayName": "PolyPage — Web Translator Extension",
  "version": "1.0.0",
  "description": "Chrome/Edge Manifest V3 web page translator with pluggable LLM / custom API providers, bilingual reading and hover tooltips.",
  "private": true,
  "type": "module",
  "scripts": {
    "icons": "node scripts/generate-icons.mjs",
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "smoke": "node scripts/smoke-test.mjs",
    "verify": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
